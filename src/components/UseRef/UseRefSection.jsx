import { Wrapper } from '../common/Envoltorio';
import { FocusScreen } from './FocusScreen';

export const UseRefSection = () => {

  return (

    <Wrapper>
      <FocusScreen title={'useRef'} />
    </Wrapper>

  )
}